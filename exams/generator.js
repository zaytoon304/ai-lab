function openExamGenerator(){history.replaceState({},'','#exams');renderExamBuilder()}function renderExamBuilder(){const levels=BOOK_DATA.levels;app.innerHTML=`<section class="card exam-head"><div><small>مولد الاختبارات</small><h2>أنشئ اختبارا جاهزا للطباعة</h2><p>اختبار عشوائي من محتوى المستوى مع نموذج إجابة منفصل.</p></div></section><section class="card exam-form"><label>المستوى<select id="examLevel">${levels.map(l=>`<option value="${l.id}">${l.title}</option>`).join('')}</select></label><label>عدد الأسئلة<select id="examCount"><option>10</option><option selected>15</option><option>20</option></select></label><label>نوع الاختبار<select id="examType"><option value="mixed">متنوع</option><option value="choice">اختيار من متعدد</option><option value="writing">قراءة وكتابة</option></select></label><button class="btn" onclick="generateExam()">إنشاء الاختبار</button></section>`}function examItems(level,count,type){const words=[...new Set(level.lessons.flatMap(l=>l.words||[]))],letters=[...new Set(level.lessons.flatMap(l=>l.letters||[]))],groups=[...new Set(level.lessons.flatMap(l=>(l.groups||[]).flat()))],pool=[...words,...letters,...groups].filter(Boolean),items=[];const addChoice=()=>{const answer=pool[Math.floor(Math.random()*pool.length)],others=shuffle(pool.filter(x=>x!==answer)).slice(0,3);items.push({kind:'choice',q:'اختر الإجابة الصحيحة:',choices:shuffle([answer,...others]),answer})};const addMissing=()=>{const word=words[Math.floor(Math.random()*words.length)]||pool[Math.floor(Math.random()*pool.length)],chars=[...word],i=Math.max(0,Math.floor(chars.length/2));const answer=chars[i];chars[i]='ـ';items.push({kind:'missing',q:`أكمل الحرف الناقص: ${chars.join('')}`,answer})};const addWrite=()=>{const word=words[Math.floor(Math.random()*words.length)]||pool[Math.floor(Math.random()*pool.length)];items.push({kind:'write',q:`اكتب الكلمة بعد سماعها: ____________`,audio:word,answer:word})};for(let i=0;i<count;i++){if(type==='choice')addChoice();else if(type==='writing')i%2?addMissing():addWrite();else{const mode=i%3;if(mode===0)addChoice();else if(mode===1)addMissing();else addWrite()}}return items}function generateExam(){const level=BOOK_DATA.levels.find(l=>l.id===document.getElementById('examLevel').value),count=Number(document.getElementById('examCount').value),type=document.getElementById('examType').value,items=examItems(level,count,type);window.currentExamItems=items;app.innerHTML=`<section class="exam-page"><header><div><h1>القاعدة الذهبية التفاعلية</h1><p>اختبار ${level.title}</p></div><div><strong>الاسم: __________________</strong><br><strong>الفصل: ________ التاريخ: ________</strong></div></header><div class="exam-meta"><span>عدد الأسئلة: ${count}</span><span>الدرجة: ______ / ${count}</span></div><main>${items.map((item,i)=>`<article class="exam-question"><h3>${i+1}. ${item.q}</h3>${item.kind==='choice'?`<div class="exam-choices">${item.choices.map((c,n)=>`<span>${String.fromCharCode(65+n)}. ${c}</span>`).join('')}</div>`:''}${item.kind==='write'?'<div class="exam-write-lines"></div>':''}</article>`).join('')}</main><footer><span>إعداد وتصميم: محمد زيتون</span><span>توقيع المعلم: ______________</span></footer></section><div class="actions no-print"><button class="btn" onclick="window.print()">طباعة الاختبار</button><button class="btn alt" onclick="showExamAnswers()">نموذج الإجابة</button><button class="btn alt" onclick="renderExamBuilder()">اختبار جديد</button></div>`}function showExamAnswers(){const items=window.currentExamItems||[];app.innerHTML=`<section class="exam-page answer-key"><header><div><h1>نموذج الإجابة</h1><p>القاعدة الذهبية التفاعلية</p></div><strong>محمد زيتون</strong></header><main>${items.map((item,i)=>`<article class="answer-row"><span>${i+1}</span><strong>${item.answer}</strong></article>`).join('')}</main></section><div class="actions no-print"><button class="btn" onclick="window.print()">طباعة النموذج</button><button class="btn alt" onclick="openExamGenerator()">رجوع</button></div>`}const examOldTeacher=renderTeacherDashboard;renderTeacherDashboard=function(){examOldTeacher();const head=document.querySelector('.teacher-head');if(head&&!document.getElementById('examBtn'))head.insertAdjacentHTML('beforeend','<button id="examBtn" class="btn exam-link" onclick="openExamGenerator()">مولد الاختبارات</button>')};window.addEventListener('hashchange',()=>{if(location.hash==='#exams')renderExamBuilder()});if(location.hash==='#exams')renderExamBuilder();