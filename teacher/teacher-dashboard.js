function teacherReports(){try{return JSON.parse(localStorage.getItem('mz-teacher-reports')||'[]')}catch{return []}}function saveTeacherReports(reports){localStorage.setItem('mz-teacher-reports',JSON.stringify(reports))}function openTeacherDashboard(){history.replaceState({},'','#teacher');renderTeacherDashboard()}function renderTeacherDashboard(){const reports=teacherReports(),totalLessons=allLessons().length,avg=reports.length?Math.round(reports.reduce((s,r)=>s+Math.round(((r.completed||[]).length/totalLessons)*100),0)/reports.length):0;app.innerHTML=`<section class="card teacher-head"><div><small>لوحة المعلم</small><h2>محمد زيتون</h2><p>اجمع تقارير الطلاب المحفوظة على أجهزتهم واعرضها في لوحة صفية واحدة.</p></div><label class="btn import-label">استيراد تقرير طالب<input id="teacherImport" type="file" accept="application/json" onchange="importStudentReport(event)" hidden></label></section><section class="stats teacher-stats"><div><b>${reports.length}</b><span>طالب</span></div><div><b>${avg}%</b><span>متوسط الإنجاز</span></div><div><b>${reports.reduce((s,r)=>s+(r.score||0),0)}</b><span>إجمالي النقاط</span></div></section><section class="card"><div class="lesson-head"><h3>تقارير الطلاب</h3>${reports.length?`<button class="btn alt" onclick="clearTeacherReports()">مسح التقارير</button>`:''}</div>${reports.length?`<div class="teacher-table-wrap"><table class="teacher-table"><thead><tr><th>الطالب</th><th>الدروس</th><th>الإنجاز</th><th>النقاط</th><th>آخر تقرير</th></tr></thead><tbody>${reports.sort((a,b)=>(b.score||0)-(a.score||0)).map(r=>{const done=(r.completed||[]).length,pct=Math.round(done/totalLessons*100);return `<tr><td><strong>${r.student||'طالب بلا اسم'}</strong></td><td>${done}/${totalLessons}</td><td><div class="mini-progress"><span style="width:${pct}%"></span></div>${pct}%</td><td>${r.score||0}</td><td>${r.exportedAt?new Date(r.exportedAt).toLocaleDateString('ar-SA'):'—'}</td></tr>`}).join('')}</tbody></table></div>`:`<div class="empty-state"><div>📊</div><h3>لا توجد تقارير بعد</h3><p>اطلب من الطالب فتح ملفه والضغط على «حفظ التقرير»، ثم استورد الملف هنا.</p></div>`}</section><div class="actions"><button class="btn alt" onclick="go('progress')">العودة إلى ملف الطالب</button></div>`}function importStudentReport(event){const file=event.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const report=JSON.parse(reader.result);if(!Array.isArray(report.completed)||typeof report.score!=='number')throw new Error('invalid');const reports=teacherReports(),key=(report.student||'')+'|'+(report.exportedAt||'');const filtered=reports.filter(r=>((r.student||'')+'|'+(r.exportedAt||''))!==key);filtered.push(report);saveTeacherReports(filtered);renderTeacherDashboard()}catch{alert('ملف التقرير غير صالح')}};reader.readAsText(file)}function clearTeacherReports(){if(confirm('هل تريد حذف جميع تقارير الطلاب من هذا الجهاز؟')){saveTeacherReports([]);renderTeacherDashboard()}}window.addEventListener('hashchange',()=>{if(location.hash==='#teacher')renderTeacherDashboard()});if(location.hash==='#teacher')renderTeacherDashboard();