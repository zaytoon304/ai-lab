function openFlashcards(){history.replaceState({},'','#flashcards');renderFlashcardBuilder()}function flashcardLessons(){return allLessons().filter(l=>(l.words&&l.words.length)||(l.letters&&l.letters.length))}function renderFlashcardBuilder(){const lessons=flashcardLessons();app.innerHTML=`<section class="card flashcard-head"><div><small>مولد البطاقات التعليمية</small><h2>أنشئ بطاقات حروف وكلمات</h2><p>بطاقات كبيرة للعرض الصفي أو التدريب الفردي.</p></div></section><section class="card flashcard-form"><label>الدرس<select id="flashLesson">${lessons.map(l=>`<option value="${l.id}">${l.levelTitle} — ${l.title}</option>`).join('')}</select></label><label>عدد البطاقات<select id="flashCount"><option>4</option><option selected>8</option><option>12</option><option>16</option></select></label><label>نمط البطاقة<select id="flashMode"><option value="word">الكلمة فقط</option><option value="trace">الكلمة مع سطر كتابة</option><option value="audio">الكلمة مع رمز الاستماع</option></select></label><button class="btn" onclick="generateFlashcards()">إنشاء البطاقات</button></section>`}function generateFlashcards(){const lesson=allLessons().find(l=>l.id===document.getElementById('flashLesson').value),count=Number(document.getElementById('flashCount').value),mode=document.getElementById('flashMode').value,pool=[...new Set([...(lesson.words||[]),...(lesson.letters||[])])],items=shuffle(pool).slice(0,count);app.innerHTML=`<section class="flashcard-sheet">${items.map(item=>`<article class="print-flashcard"><div class="flash-main">${item}</div>${mode==='trace'?`<div class="flash-trace">${item}</div>`:''}${mode==='audio'?'<div class="flash-audio">🔊</div>':''}<small>القاعدة الذهبية التفاعلية — محمد زيتون</small></article>`).join('')}</section><div class="actions no-print"><button class="btn" onclick="window.print()">طباعة البطاقات</button><button class="btn alt" onclick="renderFlashcardBuilder()">إنشاء بطاقات أخرى</button><button class="btn alt" onclick="openTeacherDashboard()">لوحة المعلم</button></div>`}const flashOldTeacher=renderTeacherDashboard;renderTeacherDashboard=function(){flashOldTeacher();const head=document.querySelector('.teacher-head');if(head&&!document.getElementById('flashcardsBtn'))head.insertAdjacentHTML('beforeend','<button id="flashcardsBtn" class="btn flashcards-link" onclick="openFlashcards()">البطاقات التعليمية</button>')};window.addEventListener('hashchange',()=>{if(location.hash==='#flashcards')renderFlashcardBuilder()});if(location.hash==='#flashcards')renderFlashcardBuilder();